#include <stdio.h>
int main() {
    int n=10;
    int  a=0,b=1,c,sum=0;
    printf("Fibonacci series:\n");
    for (int i = 0; i < n; i++) {
        printf("%d, ", a);
        sum += a;
        c = a + b;
        a = b;
        b= c;
    }
    printf("\nSum: %d\n", sum);
    return 0;
}
